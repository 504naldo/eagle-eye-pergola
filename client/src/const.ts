export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// Returns the path where unauthenticated users are redirected.
// The home page (/) hosts the login/register form.
export const getLoginUrl = () => "/";
